# Raise the open-file soft limit for the pipewire-pulse daemon.
#
# Same reasoning as ~/.config/systemd/user/pipewire.service.d/10-nofile.conf --
# pipewire-pulse also accepts client connections and is exposed to the same
# 1024-fd ceiling. See that file for the failure mode.

[Service]
LimitNOFILE=65536
