#!/bin/bash
COMMAND="/usr/bin/terminator --geometry 800x600"

if [ $(basename $0) = "terminator-floating" ]
then
	COMMAND="$COMMAND --role floating"
else
	COMMAND="$COMMAND "--working-directory="$(xcwd)"
fi
exec $COMMAND $*
