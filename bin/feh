#!/bin/bash

source $HOME/etc/defaults
target="${1:-$pictures}"
shift
if [ -f "${target}" ]
then
	target="--start-at ${target} $(dirname ${target})"
fi
/usr/bin/feh --image-bg black --geometry 1080x610 ${target} $*

