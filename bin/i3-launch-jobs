#!/bin/bash

set -e

source $HOME/etc/defaults

launch_i3job mqtt-notify.py
launch_i3job autocutsel -verbose -debug -selection CLIPBOARD

#launch_i3job autocutsel -verbose -debug -selection PRIMARY
#launch_i3job rerandr3 daemon
#launch_i3job setbg loop debian
#
#pulseaudio --check && pulseaudio --kill ; pulseaudio --start
#dropbox stop; sleep 1; dropbox start

exit 0

##############################################################################
