#!/bin/sh

# update backrgound lockscreen
setbg lock geek

# lock screen with a background picture
i3lock -i ~/.lockscreen.png -t

# also suspend if told so
if  [ "$1" = suspend ]
then
    # wait a bit to make sure it's locked
    sleep 2

    # suspend
    sudo /usr/sbin/pm-suspend
fi

