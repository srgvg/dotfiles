#!/bin/sh

# switch to the IRC window, or start it
wmctrl -a IRC || \
/usr/bin/terminator \
            --geometry 800x600 \
            --title IRC \
            --role irc \
            --command "bash -i -c irc" &
